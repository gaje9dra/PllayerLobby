"use client";

import { useActionState } from "react";
import { Button } from "@/components/ui/button";
import { createResultAction, disqualifyResultAction, updateResultAction, verifyResultAction, type ResultActionState } from "./actions";

const initialState: ResultActionState = { ok: false };
function confirmDisqualification() { return window.confirm("Disqualify this result? The result will no longer be eligible for future prize settlement and cannot be reverted to DRAFT."); }

export function ResultRow({ tournamentId, registrationId, result }: { tournamentId: string; registrationId: string; result: { id: string; rank: number; score: string; resultStatus: string } | null }) {
  const [createState, createAction, createPending] = useActionState(createResultAction, initialState);
  const [updateState, updateAction, updatePending] = useActionState(updateResultAction, initialState);
  const state = createState.message || updateState.message;
  if (!result) return <form action={createAction} className="flex flex-wrap items-end gap-2"><input type="hidden" name="tournamentId" value={tournamentId} /><input type="hidden" name="registrationId" value={registrationId} /><label className="text-xs text-slate-500">Rank<input required name="rank" inputMode="numeric" className="mt-1 w-20 rounded-lg border border-white/10 bg-slate-950 px-2 py-2 text-sm text-white" /></label><label className="text-xs text-slate-500">Score<input required name="score" inputMode="decimal" className="mt-1 w-28 rounded-lg border border-white/10 bg-slate-950 px-2 py-2 text-sm text-white" /></label><Button type="submit" disabled={createPending}>Save Draft</Button>{state ? <span className="text-xs text-slate-400">{state}</span> : null}</form>;
  if (result.resultStatus === "VERIFIED") return <div className="flex flex-wrap items-center gap-2"><span className="text-sm font-bold text-lime-200">{result.rank}</span><span className="text-sm text-slate-300">{result.score}</span><span className="rounded-full bg-lime-300/10 px-2 py-1 text-xs font-bold text-lime-200">VERIFIED</span><form action={disqualifyResultAction} onSubmit={(event) => { if (!confirmDisqualification()) event.preventDefault(); }}><input type="hidden" name="tournamentId" value={tournamentId} /><input type="hidden" name="resultId" value={result.id} /><Button type="submit" variant="secondary">Disqualify</Button></form></div>;
  if (result.resultStatus === "DISQUALIFIED") return <div className="flex items-center gap-3"><span className="text-sm text-slate-500">{result.rank} · {result.score}</span><span className="rounded-full bg-red-300/10 px-2 py-1 text-xs font-bold text-red-200">DISQUALIFIED</span></div>;
  return <div className="space-y-2"><form action={updateAction} className="flex flex-wrap items-end gap-2"><input type="hidden" name="tournamentId" value={tournamentId} /><input type="hidden" name="resultId" value={result.id} /><label className="text-xs text-slate-500">Rank<input required name="rank" defaultValue={result.rank} inputMode="numeric" className="mt-1 w-20 rounded-lg border border-white/10 bg-slate-950 px-2 py-2 text-sm text-white" /></label><label className="text-xs text-slate-500">Score<input required name="score" defaultValue={result.score} inputMode="decimal" className="mt-1 w-28 rounded-lg border border-white/10 bg-slate-950 px-2 py-2 text-sm text-white" /></label><Button type="submit" disabled={updatePending}>Update</Button></form><div className="flex flex-wrap gap-2"><form action={verifyResultAction}><input type="hidden" name="tournamentId" value={tournamentId} /><input type="hidden" name="resultId" value={result.id} /><Button type="submit">Verify</Button></form><form action={disqualifyResultAction} onSubmit={(event) => { if (!confirmDisqualification()) event.preventDefault(); }}><input type="hidden" name="tournamentId" value={tournamentId} /><input type="hidden" name="resultId" value={result.id} /><Button type="submit" variant="secondary">Disqualify</Button></form>{state ? <span className="text-xs text-slate-400">{state}</span> : null}</div></div>;
}
